export interface ILocation {
  latitude: number;
  longitude: number;
  // altitude?: number | undefined;
}

export interface ILocationResponse {
  latitude: string;
  longitude: string;
  // altitude?: number | undefined;
}
